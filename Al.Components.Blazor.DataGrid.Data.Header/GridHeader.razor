@using Al.Components.Blazor.DataGrid.Model.Enums
@inherits HandRenderComponent

    <GridColgroup DataGridModel="DataGridModel" />

    <thead>
        <tr>
            @if (DataGridModel.Rows.ShowDetail)
            {
                <th class="expander fixed">&nbsp;</th>
            }

            @if (DataGridModel.Rows.SelectableRowMode == SelectableRowMode.Multiple)
            {
                <th class="selector">&nbsp;</th>
            }

            @foreach (var column in DataGridModel.Columns.FrozenLeft.Where(x => x.Item.Visible))
            {
                <HeaderColumn DataGridModel=DataGridModel
                          ResizeArea=null
                          @key=column
                          ColumnNode=column />
            }

            @foreach (var column in DataGridModel.Columns.Frozenless.Where(x => x.Item.Visible))
            {
                <HeaderColumn DataGridModel=DataGridModel
                          ResizeArea=null
                          @key=column
                          ColumnNode=column />
            }

            @foreach (var column in DataGridModel.Columns.FrozenRight.Where(x => x.Item.Visible))
            {
                <HeaderColumn DataGridModel=DataGridModel
                          ResizeArea=null
                          @key=column
                          ColumnNode=column />
            }
            <td class="compensator">&nbsp;</td>
        </tr>
    </thead>
